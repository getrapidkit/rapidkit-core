{# Shared snippet for the RapidKit Vector Store module. #}
{% set module_name = "vector_store" %}
{% set module_title = "Vector Store" %}

# {{ module_title }} integration notes

- Canonical module: `free/ai/vector_store`
- Primary purpose: Vector index abstraction for embeddings, metadata filters, and similarity search.
- Key capabilities: Provides a stable boundary for pgvector/Qdrant-style backends., Stores embedding metadata needed for filtering and citation., Exposes health checks for index availability.
- Runtime environment keys: RAPIDKIT_VECTOR_STORE_ENABLED, RAPIDKIT_VECTOR_STORE_BACKEND, RAPIDKIT_VECTOR_STORE_DIMENSIONS

Use this snippet near the owning service/module registration point so generated projects keep module intent, operational boundaries, and security expectations visible to maintainers.
