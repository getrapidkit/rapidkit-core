from __future__ import annotations

from typing import Any

from src.modules.free.ai.rag_pipeline.rag_pipeline import {{ module_class_name }}


def module_health_status(pipeline: Any | None = None) -> dict[str, Any]:
    """Return RAG pipeline health for FastAPI variants."""

    runtime = pipeline or {{ module_class_name }}()
    return runtime.health()
